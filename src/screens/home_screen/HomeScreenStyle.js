import { StyleSheet, Text, View } from 'react-native'
import CustomColors from '../../theme/Color'

const styles = StyleSheet.create({
    container:{
        backgroundColor: CustomColors.white, flex: 1  
    }
})

export default styles